# Node modules
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Wrangler files
.wrangler/
wrangler.jsonc
wrangler.toml

# Environment files
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
# Check if .gitignore has node_modules
cat .gitignore | grep node_modules

# If not, add it
echo "node_modules/" >> .gitignore
echo "package-lock.json" >> .gitignore
echo "bun.lockb" >> .gitignore
